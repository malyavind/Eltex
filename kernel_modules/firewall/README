Модуль ядра, позволяющий блокировать входящие и исходящие UDP пакеты по номеру порта.

Usage:

$ sudo make
$ sudo insmod ./fw.ko

По умолчанию блокируются пакеты по 3344 порту. 

Изменить порт:

$ echo 'port' > /sys/class/fw-class/fw

Пример:

$ echo 1337 > /sys/class/fw-class/fw
